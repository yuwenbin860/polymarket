# Polymarket组合套利系统依赖

# 核心依赖（必需）
requests>=2.28.0
httpx>=0.24.0

# LLM提供商SDK（按需安装，选择你使用的）
# openai>=1.0.0          # OpenAI GPT系列
# anthropic>=0.18.0      # Anthropic Claude系列
# dashscope>=1.10.0      # 阿里云通义千问
# zhipuai>=2.0.0         # 智谱GLM

# 注：httpx已支持所有OpenAI兼容接口，无需额外依赖
# 包括：DeepSeek、Ollama、vLLM、OneAPI等

# Phase 2 扩展依赖
sentence-transformers>=2.2.0   # 语义相似度计算
numpy>=1.24.0                  # 向量计算

# 交互式CLI
rich>=13.0.0                   # 终端格式化、进度条、表格
questionary>=2.0.0             # 交互式提示菜单

# 可选：向量数据库（大规模市场时使用）
# chromadb>=0.4.0

# 开发依赖（可选）
# pytest>=7.0.0
# black>=23.0.0
# python-dotenv>=1.0.0
